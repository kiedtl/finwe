\\ 5
\\ 1, 1, 1

( We test ln() with the following formula:
  √S = pow(e, 0.5×ln(S)) )
word ln-sqrt [[ push @E 0.5 pop ln * pow ceil ]]

25 ln-sqrt .n cr

423 square dup ln-sqrt swap sqrt =
128 square dup ln-sqrt swap sqrt =
319 square dup ln-sqrt swap sqrt =
dbg
